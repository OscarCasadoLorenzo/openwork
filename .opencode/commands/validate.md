---
description: Run the pre-flight validation script to check credentials, API reachability, and directory structure
agent: build
---

Run the following bash command and report the results clearly:

!`bash scripts/validate.sh`

If there are failures, explain what each failure means and provide the exact steps to fix it.
If all checks pass, confirm the system is ready and remind the user to run `/smoke-test` next.
